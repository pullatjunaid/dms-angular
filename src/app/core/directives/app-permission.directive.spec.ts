import { AppPermissionDirective } from './app-permission.directive';

describe('AppPermissionDirective', () => {
  it('should create an instance', () => {
    const directive = new AppPermissionDirective();
    expect(directive).toBeTruthy();
  });
});
